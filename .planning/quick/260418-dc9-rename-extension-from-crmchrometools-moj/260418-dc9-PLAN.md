---
phase: quick
plan: 260418-dc9
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - src/popup/popup.html
  - src/popup/popup.ts
  - src/content/all-fields.ts
  - src/content/all-fields.css
  - src/content/ribbon-toolbar.ts
  - README.md
  - popup/popup.html
autonomous: true
requirements: [rename-extension]

must_haves:
  truths:
    - "The string 'MojnTools' does not appear in any user-visible string, log prefix, comment header, or documentation"
    - "The string 'CRMChromeTools' does not appear as a project label in README.md"
    - "The extension name displayed in Chrome is 'DynamicsCat'"
    - "Console log prefix is '[DynamicsCat]' in all content/popup scripts"
  artifacts:
    - path: "manifest.json"
      provides: "Extension name, description, action title — all showing DynamicsCat"
      contains: "\"name\": \"DynamicsCat\""
    - path: "src/popup/popup.html"
      provides: "Popup title and header branding"
      contains: "DynamicsCat"
    - path: "src/popup/popup.ts"
      provides: "Log prefix"
      contains: "[DynamicsCat]"
    - path: "src/content/all-fields.ts"
      provides: "Log prefix"
      contains: "[DynamicsCat]"
    - path: "src/content/all-fields.css"
      provides: "Comment header"
      contains: "DynamicsCat"
    - path: "src/content/ribbon-toolbar.ts"
      provides: "Toolbar tooltip and icon alt text"
      contains: "DynamicsCat"
    - path: "README.md"
      provides: "Documentation heading and project structure label"
      contains: "DynamicsCat"
    - path: "popup/popup.html"
      provides: "Legacy root popup branding"
      contains: "DynamicsCat"
  key_links:
    - from: "manifest.json"
      to: "Chrome extension toolbar"
      via: "\"name\" and \"default_title\" fields"
      pattern: "\"name\": \"DynamicsCat\""
    - from: "src/content/all-fields.ts"
      to: "browser console"
      via: "LOG() helper"
      pattern: "\\[DynamicsCat\\]"
---

<objective>
Rename the Chrome extension from MojnTools/CRMChromeTools to DynamicsCat across all user-visible strings, log prefixes, comment headers, and documentation.

Purpose: Consistent branding — the extension should identify itself as DynamicsCat everywhere a user or developer encounters its name.
Output: 8 files updated; zero remaining occurrences of "MojnTools" or "CRMChromeTools" in user-facing strings, log prefixes, comments, and docs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace MojnTools/CRMChromeTools → DynamicsCat in source files</name>
  <files>
    manifest.json,
    src/popup/popup.html,
    src/popup/popup.ts,
    src/content/all-fields.ts,
    src/content/all-fields.css,
    src/content/ribbon-toolbar.ts
  </files>
  <action>
Apply the following exact string substitutions in each file. Do NOT rename any files or change any import paths — only replace the brand name strings.

**manifest.json** — 3 replacements:
- `"name": "MojnTools"` → `"name": "DynamicsCat"`
- `"description": "MojnTools — Dynamics CRM 2016 helper tools for form field inspection and more."` → `"description": "DynamicsCat — Dynamics CRM 2016 helper tools for form field inspection and more."`
- `"default_title": "MojnTools"` → `"default_title": "DynamicsCat"`

**src/popup/popup.html** — 3 replacements:
- `<title>MojnTools</title>` → `<title>DynamicsCat</title>`
- `alt="MojnTools"` → `alt="DynamicsCat"`
- `<span class="header-title">MojnTools</span>` → `<span class="header-title">DynamicsCat</span>`

**src/popup/popup.ts** — 1 replacement:
- `console.error(\`[MojnTools]` → `console.error(\`[DynamicsCat]`

**src/content/all-fields.ts** — 1 replacement:
- `const LOG = (msg: string) => console.log('[MojnTools]', msg);` → `const LOG = (msg: string) => console.log('[DynamicsCat]', msg);`

**src/content/all-fields.css** — 1 replacement:
- `/* ── MojnTools — Show All Fields Panel` → `/* ── DynamicsCat — Show All Fields Panel`

**src/content/ribbon-toolbar.ts** — 2 replacements:
- `wrapper.title = 'MojnTools';` → `wrapper.title = 'DynamicsCat';`
- `icon.alt = 'MojnTools';` → `icon.alt = 'DynamicsCat';`
  </action>
  <verify>
    <automated>
      Select-String -Path "manifest.json","src\popup\popup.html","src\popup\popup.ts","src\content\all-fields.ts","src\content\all-fields.css","src\content\ribbon-toolbar.ts" -Pattern "MojnTools|CRMChromeTools" | Measure-Object | Select-Object -ExpandProperty Count
    </automated>
  </verify>
  <done>
    Command returns 0. No occurrences of "MojnTools" or "CRMChromeTools" remain in any of the 6 source files.
    manifest.json shows "name": "DynamicsCat", "default_title": "DynamicsCat", and description starting with "DynamicsCat —".
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace MojnTools/CRMChromeTools → DynamicsCat in docs and legacy popup</name>
  <files>
    README.md,
    popup/popup.html
  </files>
  <action>
Apply the following exact string substitutions. Do NOT rename the folder label in the project structure tree (it reflects the actual repo folder name which must not change) — ONLY change the heading `# MojnTools` and the literal string `CRMChromeTools/` used as the project root label in the tree.

**README.md** — 2 replacements:
- `# MojnTools` → `# DynamicsCat`
- `CRMChromeTools/` (the project structure tree root label on its own line, e.g. `CRMChromeTools/`) → `DynamicsCat/`

Note: The line reads:
```
CRMChromeTools/
```
Replace only that label. The subdirectory paths and file descriptions beneath it are unchanged.

**popup/popup.html** (root-level legacy file) — 3 replacements:
- `<title>MojnTools</title>` → `<title>DynamicsCat</title>`
- `alt="MojnTools"` → `alt="DynamicsCat"`
- `<span class="header-title">MojnTools</span>` → `<span class="header-title">DynamicsCat</span>`
  </action>
  <verify>
    <automated>
      Select-String -Path "README.md","popup\popup.html" -Pattern "MojnTools|CRMChromeTools" | Measure-Object | Select-Object -ExpandProperty Count
    </automated>
  </verify>
  <done>
    Command returns 0. README.md heading is "# DynamicsCat". Project structure tree root is "DynamicsCat/". popup/popup.html title, alt, and header span all read "DynamicsCat".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Chrome extension store | Extension name/description are user-visible in Chrome UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-dc9-01 | Tampering | manifest.json name field | accept | Name change is intentional; no security surface — display-only string |
| T-dc9-02 | Information Disclosure | console log prefix | accept | Log prefix is developer-facing only; no sensitive data exposed |
</threat_model>

<verification>
Run a final repo-wide scan after both tasks complete to confirm zero residual occurrences:

```powershell
Select-String -Path "manifest.json","src\popup\popup.html","src\popup\popup.ts","src\content\all-fields.ts","src\content\all-fields.css","src\content\ribbon-toolbar.ts","README.md","popup\popup.html" -Pattern "MojnTools|CRMChromeTools"
```

Expected: no output (zero matches).
</verification>

<success_criteria>
- Zero occurrences of "MojnTools" in all 8 files
- Zero occurrences of "CRMChromeTools" in all 8 files
- manifest.json `"name"` is `"DynamicsCat"`, `"default_title"` is `"DynamicsCat"`
- README.md h1 heading is `# DynamicsCat`
- All `[MojnTools]` console log prefixes replaced with `[DynamicsCat]`
- No file renames, no import path changes, no module names changed
</success_criteria>

<output>
After completion, create `.planning/quick/260418-dc9-rename-extension-from-crmchrometools-moj/260418-dc9-SUMMARY.md` with:
- Files changed (list)
- Replacement count per file
- Final verification scan output (0 matches)
- Commit hash
</output>
